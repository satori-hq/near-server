require('dotenv').config()
const regeneratorRuntime = require("regenerator-runtime");
const assert = require('assert');
const { near } = require('../index');

describe('NFT Series', function () {
	this.timeout(10000);

	it('should run', async function () {
		console.log(near)
	})

})
